name: 'varejinho'
version: '1.0.0'
profile: 'varejinho'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  varejinho:
    +catalog: varejinho
    +schema: gold
    +materialized: table
    +file_format: delta